# Otimização do Claude Code (harness, agentes, skills, hooks, docs gerados)
.claude/
.harness/
Docs/
spec/
CLAUDE.md

# Legado: versiona o código antigo, mas não o ambiente virtual nem caches
legado/.venv/
legado/.pytest_cache/

# macOS
.DS_Store
